<h1>新規投稿</h1>
<!-- form_with部分 -->
<%= form_with model: @user, local:true do |f| %>
  <h4>name</h4>
  <%= f.text_field :name %>
  <h4>opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>